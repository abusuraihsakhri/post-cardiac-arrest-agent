version: '3.8'

services:
  post-cardiac-arrest-engine:
    build: .
    container_name: post-cardiac-arrest-engine
    ports:
      - "8000:8000"
    environment:
      - AUDIT_SECRET_KEY=post-cardiac-arrest-engine-prod-audit-key-2026
      - MODEL_PROVIDER=mock
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 5s
      retries: 3
